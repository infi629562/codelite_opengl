# -*- CMakeLists.txt generated by CodeLite IDE. Do not edit by hand -*-

cmake_minimum_required(VERSION 2.8.11)

project(opengl_env)

# Define some variables
set(PROJECT_PATH "${CMAKE_SOURCE_DIR}/opengl_env")



#{{{{ User Code 1
# Place your code here
#}}}}

include_directories(
    .
    include

)


# Compiler options
execute_process(COMMAND 
    pkg-config --cflags glu glew glfw3
    OUTPUT_VARIABLE
    CL_TMP_VAR
    OUTPUT_STRIP_TRAILING_WHITESPACE)
string(STRIP ${CL_TMP_VAR} CL_VAR_1)
execute_process(COMMAND 
    pkg-config --cflags glu glew glfw3
    OUTPUT_VARIABLE
    CL_TMP_VAR
    OUTPUT_STRIP_TRAILING_WHITESPACE)
string(STRIP ${CL_TMP_VAR} CL_VAR_2)
add_definitions(${CL_VAR_1})
add_definitions(${CL_VAR_2})
add_definitions(-g)
add_definitions(-O0)
add_definitions(-Wall)

# Linker options
execute_process(COMMAND 
    pkg-config --libs glu glew glfw3
    OUTPUT_VARIABLE
    CL_TMP_VAR
    OUTPUT_STRIP_TRAILING_WHITESPACE)
string(STRIP ${CL_TMP_VAR} CL_VAR_3)
set(LINK_OPTIONS ${CL_VAR_3})


if(WIN32)
    # Resource options
endif(WIN32)

# Library path
set(CMAKE_LDFLAGS "${CMAKE_LDFLAGS} -L. ")

# Define the C sources
set ( C_SRCS
    ${CMAKE_SOURCE_DIR}/opengl_env/src/glad.c
)

# Define the CXX sources
set ( CXX_SRCS
    ${CMAKE_SOURCE_DIR}/opengl_env/src/main.cpp
)

set_source_files_properties(
    ${CXX_SRCS} PROPERTIES COMPILE_FLAGS 
    " -g -O0 -Wall")

set_source_files_properties(
    ${C_SRCS} PROPERTIES COMPILE_FLAGS 
    " -g -O0 -Wall")

if(WIN32)
    enable_language(RC)
    set(CMAKE_RC_COMPILE_OBJECT
        "<CMAKE_RC_COMPILER> ${RC_OPTIONS} -O coff -i <SOURCE> -o <OBJECT>")
endif(WIN32)



#{{{{ User Code 2
# Place your code here
#}}}}

add_executable(opengl_env ${RC_SRCS} ${CXX_SRCS} ${C_SRCS})
target_link_libraries(opengl_env ${LINK_OPTIONS})

target_link_libraries(opengl_env
    dl
)



#{{{{ User Code 3
# Place your code here
#}}}}

